package com.akzv.resexecutor.resilientExecutor;

public class TimeoutConfig {
    public long timeoutMillis;

    public TimeoutConfig(long timeoutMillis) {
        this.timeoutMillis = timeoutMillis;
    }
}